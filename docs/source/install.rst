=======================
Installing LabTerm
=======================
WIP

Clone the repository anywhere in your computer by running ``git clone https://github.com/mVenez/labterm.git``, then navigate to the repository root directory and install it by running ``python -m pip install .``.
